#include <math.h>
#include <stdio.h>

struct points {
  int x;
  int y;
};

int main() {
  struct points p1, p2;

  printf("Enter x for p1: ");
  scanf("%d", &points.x);

  printf("Enter y for p1: ");
  scanf("%d", &points.y);

  printf("Enter x for p2: ");
  scanf("%d", &points.x);

  printf("Enter y for p2: ");
  scanf("%d", &points.y);

  int distance =
      sqrt(((p1.x - p2.x) * (p1.x - p2.x)) + ((p1.y - p2.y) * (p1.y - p2.y)))

          printf("distance: %d", distance);
}
